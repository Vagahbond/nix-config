(include "./bar/top/eww.yuck")
; (include ".bar/middle/eww.yuck")
(include "./bar/bottom/eww.yuck")

(deflisten display_mode    
                    :initial '{ }'
                    "tail -F /tmp/display_mode 2> /dev/null" )

;; Bar Widgets ;;
(defwidget bar []
	(box :class "eww_bar ${display_mode.mode}" 
		 :orientation "v" 
(top)
(bottom)))

